class Solution {
public:
    int repeatedNTimes(vector<int>& nums) {
        unordered_set<int> seen;
        for(int num:nums)
        {
            if(seen.count(num)==1)  return num;
            seen.insert(num);
        }
        return 0;
    }
};
