class Solution {
public:
    int arrayPairSum(vector<int>& nums) {
        int maximum_sum=0;
        sort(nums.begin(),nums.end());
        for(int i=0;i<nums.size();i+=2)
        {
            maximum_sum+=nums[i];
        }
        return maximum_sum;
    }
};
